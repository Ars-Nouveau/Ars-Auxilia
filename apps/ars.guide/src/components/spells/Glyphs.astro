---
import Glyph from "./Glyph.astro";

interface Props {
  glyphs: string[];
  size?: number;
}

const { glyphs, size } = Astro.props;
---
<Fragment>
    {glyphs.map(glyph => <Glyph classes="me-1" glyph={glyph} size={size} />)}
</Fragment>
